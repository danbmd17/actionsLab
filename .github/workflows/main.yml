name: My First Workflow
on:
  push:
    branches: [main]
    paths-ignore: [.github/**]
  schedule:
    - cron: "1,31 8-18 * * 1-5"
  workflow_dispatch: 

jobs:
  First-Job:
    runs-on: ubuntu-latest
    permissions: read-all
    steps:
      - run: 
        echo "${{ github.event_name }}"
        echo "${{ github.ref }}"
